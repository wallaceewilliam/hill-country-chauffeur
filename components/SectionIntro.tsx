type Props = { eyebrow: string; title: string; text?: string; centered?: boolean };
export default function SectionIntro({ eyebrow, title, text, centered }: Props) { return <div className={centered ? "mx-auto max-w-2xl text-center" : "max-w-2xl"}><p className="eyebrow">{eyebrow}</p><h2 className="mt-4 font-serif text-4xl leading-[1.05] text-olive sm:text-5xl">{title}</h2>{text && <p className="mt-5 text-base leading-7 text-ink/70">{text}</p>}</div>; }
